development:
    driver: postgres
    open: dbname=android-library-db sslmode=disable
test:
    driver: postgres
    open: dbname=android-library-db_test sslmode=disable
production:
    driver: postgres
    open: $DATABASE_URL
